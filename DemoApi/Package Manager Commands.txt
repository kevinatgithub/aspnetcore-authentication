This are the commands that you need to run in Package Manager Console

// make sure connection string is properly configured

1. Add-Migration "added city table"

2. Update-Database

// this will add Identity migrations and create the tables

3. Add-Migration "added identity tables"

4. Update-Database